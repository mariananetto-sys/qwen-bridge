services:
  chatgpt-bridge:
    build: .
    container_name: chatgpt-bridge
    restart: unless-stopped
    env_file: .env
    ports:
      - "3001:3001"
    volumes:
      - chatgpt-state:/data
    shm_size: "1gb"

volumes:
  chatgpt-state:
